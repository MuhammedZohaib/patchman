id: reset-host-header-trust
mode: auth-review
summary: Password reset email builds link from request host header.
input: |
  const resetUrl = `${req.protocol}://${req.get('host')}/reset?token=${token}`
expected_findings:
  - title: Password reset link trusts request host header
    severity: High
    confidence: High
