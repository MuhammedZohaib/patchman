# /api-review

Focused review of REST, GraphQL, RPC, and webhook surfaces.

## Prompt template

```text
Review the API surface in {paths}. Check authn/authz, object scoping, rate limiting, mass assignment, filter abuse, pagination leakage, input validation, error exposure, headers, and webhook verification.
```
